{
  "name": "desafio-modulo5",
  "version": "1.0.0",
  "license": "MIT",
  
    "scripts": {
      "dev": "parcel serve ./src/index.html",
      "build": "parcel build ./src/index.html --dist-dir build --public-url /desafio-mod5",
      "deploy": "gh-pages -d build",
      "build-and-deploy": "npm run build && npm run deploy"
    },
    
  "devDependencies": {
    "@types/node": "^18.15.5",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
